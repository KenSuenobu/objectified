{
  "name": "apiome-mcp",
  "version": "0.3.0",
  "private": true,
  "description": "Yarn workspace anchor for the apiome-mcp Python package (uv + pyproject.toml).",
  "scripts": {
    "build": "uv sync --frozen",
    "test": "uv sync --frozen && uv run ruff check . && uv run ruff format --check . && uv run mypy -p apiome_mcp && uv run pytest"
  }
}
