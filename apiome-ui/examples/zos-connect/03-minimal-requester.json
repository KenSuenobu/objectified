{
  "_comment": "Minimal z/OS Connect descriptor — the smallest canonical requester: an `apiRequester` block naming the artifact, the target `api` block, and a single mapped operation. Everything else (language, path parameters, programs) is optional.",
  "apiRequester": {
    "name": "PingRequester",
    "version": "1.0.0"
  },
  "api": {
    "title": "Ping API",
    "specification": "openapi-3.0",
    "basePath": "https://api.example.com/ping"
  },
  "operations": [
    {
      "operationId": "ping",
      "method": "GET",
      "path": "/ping",
      "responseStructure": "PING-RESPONSE"
    }
  ]
}
