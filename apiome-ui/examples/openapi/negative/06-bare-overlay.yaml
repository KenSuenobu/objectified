overlay: 1.0.0
info:
  title: Public-audience overrides, uploaded without a base
  version: 1.0.0
extends: ./openapi.yaml
actions:
  - target: $.info
    description: Replace the description for the public audience.
    update:
      description: Public production surface.
  - target: $.paths['/internal/reindex']
    description: Drop the internal maintenance endpoint.
    remove: true
