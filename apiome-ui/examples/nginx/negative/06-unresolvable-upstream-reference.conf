# proxy_pass names upstream `orders_backend`, and no upstream block declares it — not
# here and not in any included file supplied with it.
server {
    listen 443 ssl;
    server_name api.example.com;

    location /v2/orders {
        proxy_pass http://orders_backend;
        proxy_set_header Host $host;
    }
}
