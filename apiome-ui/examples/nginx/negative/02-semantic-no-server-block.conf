# Directives only, no server block: nothing here declares a listener, a host or a
# location, so no route can be inferred.
worker_processes auto;
error_log /var/log/nginx/error.log warn;

events {
    worker_connections 1024;
}

http {
    sendfile on;
    keepalive_timeout 65;
    gzip on;
}
