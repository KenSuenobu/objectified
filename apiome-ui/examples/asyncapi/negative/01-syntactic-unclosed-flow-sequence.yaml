asyncapi: 2.6.0
info:
  title: Broken Events
  version: 1.0.0
channels:
  user/signedup:
    publish:
      operationId: onUserSignedUp
      message:
        payload:
          type: object
          required: [id, email
          properties:
            id:
              type: string
