FORMAT: 1A

# Ping API

The resource header below opens its URI-template bracket but never closes
it, so the resource line cannot be parsed.

## Ping [/ping

### Check liveness [GET

+ Response 200 (application/json)
