/// `missing.pkl` is not in the fileset, so `missing.Address` can never resolve. The
/// import must report a named unresolved reference rather than silently typing the
/// member as `Any`.
module example.shipping

import "missing.pkl"

class Shipment {
  shipmentId: String
  destination: missing.Address
}
