@0xa1b2c3d4e5f60718;

# A grammatically well-formed schema containing only constants — no struct,
# enum, or interface definitions to import.
const maxRetries :UInt8 = 3;
const defaultTimeoutMs :UInt32 = 5000;
