// Imports a module that does not exist anywhere in the fileset.
syntax = "proto3";

package acme.orders.v1;

import "acme/common/money.proto";

message Order {
  string id = 1;
  acme.common.Money total = 2;
}
