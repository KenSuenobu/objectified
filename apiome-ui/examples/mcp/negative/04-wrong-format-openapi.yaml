openapi: 3.1.0
info:
  title: Tickets API
  version: 1.4.0
paths:
  /tickets:
    get:
      operationId: searchTickets
      summary: Search tickets
      parameters:
        - name: query
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Matching tickets
