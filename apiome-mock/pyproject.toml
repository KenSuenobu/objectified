[project]
name = "apiome-mock"
version = "0.13.0"
description = "Objectified mock server runtime for Apiome (FastAPI)."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Apiome Team" }]
keywords = ["fastapi", "apiome", "openapi", "mock"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "apiome-mcp>=0.2.0",
    "apiome-rest>=1.0.53",
    "bcrypt==4.2.0",
    "grpcio>=1.66.0",
    "grpcio-reflection>=1.66.0",
    "httpx>=0.28.1",
    "protobuf>=5.0.0",
    "jsonschema>=4.21.0",
    "psycopg[binary,pool]>=3.2.0",
    "pydantic>=2.9.2",
    "pydantic-settings>=2.5.2",
    "structlog>=24.4.0",
    "uvicorn[standard]>=0.32.0",
]

[project.scripts]
apiome-mock = "apiome_mock.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/apiome_mock"]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.8.0",
    "mypy>=1.13.0",
    "pyyaml>=6.0.2",
    "types-jsonschema>=4.21.0",
    "types-protobuf>=5.0.0",
    "types-grpcio-reflection>=1.0.0",
]

[tool.ruff]
line-length = 120
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_configs = true
explicit_package_bases = true
mypy_path = ["src", "../apiome-rest/src", "../apiome-mcp/src"]

# The serverless adapter (#4743, PMR-1.3) imports azure.functions lazily, only to build a response
# object inside a deployed Azure Function. The SDK is deliberately not a dependency of the runtime
# — installing it would put an Azure package in every mock deployment — so it has no stubs here.
[[tool.mypy.overrides]]
module = ["azure.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "../apiome-rest/src", "../apiome-mcp/src"]

[tool.uv.sources]
apiome-mcp = { path = "../apiome-mcp", editable = true }
apiome-rest = { path = "../apiome-rest", editable = true }
