/**
 * Re-export of the promoted Try It response presentation helpers — DWX-4.2 (private-suite#2691).
 *
 * `apiome-ui/lib/tryit/response.ts` owns how a relayed response is classified, pretty-printed and
 * measured, so the studio's viewer and Browse's phrase the same outcome the same way.
 */

export * from '../../../apiome-ui/lib/tryit/response';
