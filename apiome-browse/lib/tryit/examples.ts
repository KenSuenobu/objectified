/**
 * Re-export of the promoted Try It example helpers — DWX-4.7 (private-suite#2696).
 *
 * Example collection and editor formatting now live in `apiome-ui/lib/tryit/examples.ts`, so the
 * designer's console prefills a request body from the document by the same reading Browse's panel
 * uses — including its rule that a variant declaring no example prefills nothing at all.
 */

export * from '../../../apiome-ui/lib/tryit/examples';
