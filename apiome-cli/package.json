{
  "name": "@apiome/cli",
  "version": "0.23.0",
  "private": true,
  "description": "Command-line client for the Apiome REST API",
  "scripts": {
    "install:py": "uv venv --allow-existing && uv sync",
    "build": "yarn install:py && echo 'apiome-cli build complete'",
    "cli:build": "yarn install:py && echo 'apiome-cli build complete'",
    "test": "uv run pytest tests/ -v && node --test test/*.test.mjs",
    "cli:test": "uv run pytest tests/ -v && node --test test/*.test.mjs",
    "lint": "uv run ruff check src/ tests/",
    "cli:lint": "uv run ruff check src/ tests/",
    "run": "bash run.sh",
    "test:scaffold": "node --test test/scaffold.test.mjs"
  }
}
